# Nivora marketing site — optional overrides.
# The app ships with safe production defaults (public Supabase anon key + API),
# so these are only needed to point at a different project or local backend.

# Public Supabase project (anon key is browser-safe; RLS restricts it to
# reading published blog posts only).
VITE_SUPABASE_URL=https://agpjxjujzjzasgizpphz.supabase.co
VITE_SUPABASE_ANON_KEY=

# Nivora backend (handles double opt-in signup + sends the confirmation mail).
VITE_NIVORA_API=https://nivora-api.srv938837.hstgr.cloud
